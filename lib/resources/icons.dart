import 'package:flutter/material.dart';

class AppIcons {
  const AppIcons._();

  static const themeIcon = Icon(Icons.wb_sunny);
  static const editIcon = Icon(Icons.edit);
  static const deleteIcon = Icon(Icons.delete);
  static const addIcon = Icon(Icons.add);
}
